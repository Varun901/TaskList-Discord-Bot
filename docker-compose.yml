version: "3.9"

services:
  taskbot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - taskbot_data:/app/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  taskbot_data:
